import styled from "@emotion/styled";
import React from "react";

const VerticalCard: React.FC = () => {
  return <Wrapper>VerticalCard</Wrapper>;
};

export default VerticalCard;

const Wrapper = styled.div``;
